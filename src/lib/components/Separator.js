import React from "react";

/**
 * A Separator is a line used to separate contexts on an container.
 */
const Separator = () => {
  return <div className="sn-separator" />;
};

export default Separator;
